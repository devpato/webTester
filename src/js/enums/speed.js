/**
 * Common Colors types used in the pageReport class (speed)
 * @typedef accountPerformance
 * @property {string} FAST
 * @property {string} AVERAGE
 * @property {string} SLOW
 * @property {string} NONE
 * @readonly
 * @enum string
 */

export const SPEED = Object.freeze({
    FAST: "#4CAF50",
    AVERAGE: "#FFAE1A",
    SLOW: '#FF6961',
    NONE: "#DC143C",
});